-in studentContext i make a state for students , so where ever i need them only i import this state so IOW getStudents API only will be called at first render of prijec via useEffect then in anywhere i need studnts i use their state , but in case i will delete/update/add (CRUD ops) i will call directlly their APIs which internally they will update students state , surther more enhancment in case of GET directlly invoke its API not only depend on its state to avoid the issues like missync ....

-futher enhancment depend on the return value(obj) from aPI to detect errors ...