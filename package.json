{
  "name": "json-server-api",
  "version": "1.0.0-beta.15",
  "scripts": {
    "start": "json-server --watch studentsDB.json --host 0.0.0.0 --port 5000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
